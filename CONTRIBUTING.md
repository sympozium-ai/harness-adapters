# Contributing an adapter

An adapter PR must include an immutable upstream pin, SBOM/vulnerability scan,
owner, support tier, capability declaration, conformance output, and a
digest-pinned installation manifest. Do not add runtime download scripts,
unbounded host mounts, default credentials, or unreviewed upstream installers.
